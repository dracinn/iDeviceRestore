<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <application
        android:name=".IDeviceRestoreApplication"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:label="${appLabel}"
        android:theme="@style/Theme.IDeviceRestore">

        <!-- Clean, presentation-only launcher. No backend behavior is wired into this activity. -->
        <activity
            android:name=".MockupActivity"
            android:exported="true"
            android:label="${appLabel}">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Preserved implementation/core entry points remain non-launcher while UI is rebuilt. -->
        <activity android:name=".MainActivity" android:exported="true" android:label="${appLabel}" />
        <activity android:name=".LogsActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity
            android:name=".BootDiagnosticsActivity"
            android:exported="false"
            android:label="${appLabel} Boot Diagnostics"
            android:taskAffinity="${applicationId}.bootdiagnostics" />

        <service
            android:name=".FirmwareDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity-alias
            android:name=".UsbAttachActivityAlias"
            android:enabled="true"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity-alias>
    </application>
</manifest>
